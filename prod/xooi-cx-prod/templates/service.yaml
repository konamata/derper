apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-cx-prod.fullname" . }}
  labels: {{- include "xooi-cx-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector: {{- include "xooi-cx-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-cx-prod.fullname" . }}-preview
  labels:
    {{- include "xooi-cx-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-cx-prod.selectorLabels" . | nindent 4 }}