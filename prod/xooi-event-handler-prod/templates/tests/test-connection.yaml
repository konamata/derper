apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-event-handler-prod.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-event-handler-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-event-handler-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
