apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-event-handler-prod.fullname" . }}
  labels:
    {{- include "xooi-event-handler-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "xooi-event-handler-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-event-handler-prod.fullname" . }}-preview
  labels:
    {{- include "xooi-event-handler-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-event-handler-prod.selectorLabels" . | nindent 4 }}