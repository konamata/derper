apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-cx-mail-prod.fullname" . }}
  labels:
    {{- include "xooi-cx-mail-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "xooi-cx-mail-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-cx-mail-prod.fullname" . }}-preview
  labels:
    {{- include "xooi-cx-mail-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "xooi-cx-mail-prod.selectorLabels" . | nindent 4 }}