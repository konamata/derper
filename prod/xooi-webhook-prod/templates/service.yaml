apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-webhook-prod.fullname" . }}
  labels: {{- include "xooi-webhook-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector: {{- include "xooi-webhook-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-webhook-prod.fullname" . }}-preview
  labels:
    {{- include "xooi-webhook-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-webhook-prod.selectorLabels" . | nindent 4 }}