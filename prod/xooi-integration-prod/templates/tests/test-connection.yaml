apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-integration-prod.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-integration-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-integration-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
