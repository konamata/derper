apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-image-edit-prod.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-image-edit-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-image-edit-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
