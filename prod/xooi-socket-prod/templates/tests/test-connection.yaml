apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-socket-prod.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-socket-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-socket-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
