apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-metabase-filenders.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-metabase-filenders.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-metabase-filenders.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
