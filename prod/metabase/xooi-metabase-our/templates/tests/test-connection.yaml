apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-metabase-our.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-metabase-our.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-metabase-our.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
