apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-metabase-tif.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-metabase-tif.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-metabase-tif.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
