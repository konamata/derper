apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-metabase-tif.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "xooi-metabase-tif.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "xooi-metabase-tif.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-metabase-tif.fullname" . }}-preview
  labels:
    {{- include "xooi-metabase-tif.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-metabase-tif.selectorLabels" . | nindent 4 }}
