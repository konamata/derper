{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "xooi-metabase-utikad.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "xooi-metabase-utikad.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
