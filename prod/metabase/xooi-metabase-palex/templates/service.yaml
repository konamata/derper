apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-metabase-palex.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "xooi-metabase-palex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "xooi-metabase-palex.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-metabase-palex.fullname" . }}-preview
  labels:
    {{- include "xooi-metabase-palex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-metabase-palex.selectorLabels" . | nindent 4 }}
