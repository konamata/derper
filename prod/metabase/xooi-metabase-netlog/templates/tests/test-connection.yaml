apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-metabase-netlog.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-metabase-netlog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-metabase-netlog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
