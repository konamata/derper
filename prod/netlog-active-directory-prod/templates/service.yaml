apiVersion: v1
kind: Service
metadata:
  name: {{ include "netlog-active-directory-prod.fullname" . }}
  labels:
    {{- include "netlog-active-directory-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "netlog-active-directory-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "netlog-active-directory-prod.fullname" . }}-preview
  labels:
    {{- include "netlog-active-directory-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netlog-active-directory-prod.selectorLabels" . | nindent 4 }}