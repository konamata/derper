apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netlog-active-directory-prod.fullname" . }}-test-connection"
  labels:
    {{- include "netlog-active-directory-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netlog-active-directory-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
