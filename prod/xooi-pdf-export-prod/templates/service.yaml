apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-pdf-export-prod.fullname" . }}
  labels:
    {{- include "xooi-pdf-export-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  clusterIP: {{ .Values.service.clusterIP }}
  selector:
    {{- include "xooi-pdf-export-prod.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "xooi-pdf-export-prod.fullname" . }}-preview
  labels:
    {{- include "xooi-pdf-export-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xooi-pdf-export-prod.selectorLabels" . | nindent 4 }}