apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-notification-prod.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-notification-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-notification-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
