apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xooi-com.fullname" . }}-test-connection"
  labels:
    {{- include "xooi-com.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xooi-com.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
